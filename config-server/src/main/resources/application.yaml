spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
#        git:
#          uri: ""
#          username: ""
#          password: ""
#          default-label: ""
#          search-paths: ""
        native:
          search-locations:
            - file:/app/config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8888
